import React, { Component } from 'react';
import Header from '../component/Header';

class Game extends Component {
  render() {
    return (
      <div>
        <Header />
      </div>
    );
  }
}

export default Game;
